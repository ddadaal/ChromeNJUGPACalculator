export interface Course {
    number: string,
    chineseName: string,
    englishName: string,
    type: string,
    score: number, // 成绩
    credit: number, // 学分
    comment: string,
    exchangeScore: string
}
